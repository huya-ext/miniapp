function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }

import React from 'react';

/**
 *  1. ployfill difference between web and rn   
 */
function ployfillScrollView(webComponents) {
  var _class, _temp;

  var RawScrollView = webComponents.ScrollView;
  webComponents.ScrollView = (_temp = _class =
  /*#__PURE__*/
  function (_RawScrollView) {
    _inheritsLoose(Web_Ployfill_ScrollView, _RawScrollView);

    function Web_Ployfill_ScrollView(props) {
      return _RawScrollView.call(this, props) || this;
    }

    var _proto = Web_Ployfill_ScrollView.prototype;

    _proto.render = function render() {
      return React.createElement(RawScrollView, this.props);
    };

    return Web_Ployfill_ScrollView;
  }(RawScrollView), _class.displayName = "HyScrollViewWeb", _class.defaultProps = {
    scrollEventThrottle: 16
  }, _temp);
}

var ployfillComponentQueue = [ployfillScrollView];
export default function ployfill(webComponents) {
  ployfillComponentQueue.forEach(function (ployfillFn) {
    ployfillFn(webComponents);
  });
  return webComponents;
}