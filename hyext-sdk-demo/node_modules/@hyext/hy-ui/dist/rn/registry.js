import { AppRegistry } from 'react-native';
export default (function (componentName, component) {
  // eslint-disable-next-line no-undef
  if (!__DEV__) {
    console.log = function () {};
  }

  AppRegistry.registerComponent(componentName, function () {
    return component;
  });
});