import SvgUri from 'react-native-svg-uri';
import React from 'react';
import { processImageComSrc, looseClone } from '../../../share/common/utils';

function SvgImage(props) {
  var cloneProps = looseClone(props);
  processImageComSrc(cloneProps);
  return React.createElement(SvgUri, cloneProps);
}

export default SvgImage;