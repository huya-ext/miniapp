import React from 'react';
import { mergeStyle, looseClone } from '../../../share/common/utils';
import webComponents from '../../../share/components';

function SvgImage(props) {
  var width = props.width,
      height = props.height;
  var cloneProps = looseClone(props);
  var extraStyle = {};

  if (width > 0) {
    extraStyle.width = parseFloat(width);
    delete cloneProps.width;
  }

  if (height > 0) {
    extraStyle.height = parseFloat(height);
    delete cloneProps.height;
  }

  if (Object.keys(extraStyle).length > 0) {
    cloneProps.style = mergeStyle(cloneProps.style, extraStyle);
  }

  return React.createElement(webComponents.Image, cloneProps);
}

export default SvgImage;